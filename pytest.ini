[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
addopts = -q --strict-markers
markers =
    integration: tests bout en bout via l'API HTTP
    unit: tests unitaires sans base de donnees
